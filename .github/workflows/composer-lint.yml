name: "Composer Lint"

on:
  pull_request:
    branches:
      - "master"
    paths:
      - .github/workflows/composer-lint.yml
      - composer.json
  push:
    branches:
      - "master"
    paths:
      - .github/workflows/composer-lint.yml
      - composer.json

jobs:
  composer-lint:
    name: "Composer Lint"
    uses: "doctrine/.github/.github/workflows/composer-lint.yml@3.0.0"
